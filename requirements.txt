# Lower bounds rather than hard pins: the pinned-back versions predate
# Python 3.14 and have no wheels for it, forcing a Rust build of pydantic-core.
fastapi>=0.115.6
uvicorn[standard]>=0.34.0
pydantic>=2.12.0
httpx>=0.28.1
python-dotenv>=1.0.1
